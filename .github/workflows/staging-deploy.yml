name: Deploy to Staging

on:
  push:
    branches: [ develop, staging ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          
      - name: Setup Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: fredfix
          
      - name: Deploy to Staging Cloud Run
        run: |
          gcloud run deploy chatterfix-cmms-staging \
            --source . \
            --region us-central1 \
            --set-env-vars="USE_FIRESTORE=true,OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }},GOOGLE_CLOUD_PROJECT=fredfix,ENVIRONMENT=staging" \
            --memory=2Gi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5 \
            --allow-unauthenticated \
            --tag=staging
            
      - name: Get Staging URL
        id: staging-url
        run: |
          STAGING_URL=$(gcloud run services describe chatterfix-cmms-staging --region us-central1 --format='value(status.url)')
          echo "staging_url=$STAGING_URL" >> $GITHUB_OUTPUT
          echo "🚀 Staging deployed at: $STAGING_URL"
          
      - name: Run Staging Tests
        run: |
          STAGING_URL=${{ steps.staging-url.outputs.staging_url }}
          
          echo "🧪 Testing staging deployment..."
          
          # Basic health check
          curl -f "$STAGING_URL" --max-time 30
          
          # Test API endpoints
          curl -f "$STAGING_URL/health" --max-time 30 || echo "Health endpoint not found"
          
          echo "✅ Staging tests passed"
          
      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const stagingUrl = "${{ steps.staging-url.outputs.staging_url }}";
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🚀 **Staging Deployment Ready**\n\n**Preview URL:** ${stagingUrl}\n\n**Test your changes:**\n- 🌐 Landing Page: ${stagingUrl}\n- 📊 Analytics: ${stagingUrl}/analytics/dashboard\n- 🔌 IoT: ${stagingUrl}/iot/sensors\n\n🤖 Generated by GitHub Actions`
            });