FROM python:3.11-slim

WORKDIR /app

# Copy requirements first for better caching
COPY parts_service_requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy application code
COPY parts_service.py .

# Expose port
EXPOSE 8080

# Run the application
CMD ["python", "parts_service.py"]