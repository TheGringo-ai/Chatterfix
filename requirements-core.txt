# ChatterFix CMMS - Core Web Application Dependencies
# Lightweight version for fast container builds (~2 minutes vs 15+ minutes)
# Heavy ML/OCR dependencies moved to separate microservices

# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
requests>=2.32.4
jinja2>=3.1.2
python-multipart>=0.0.18
aiofiles>=23.2.1

# HTTP client for calling OCR and AI Team microservices
httpx>=0.25.0

# Database - Firebase/Firestore (Primary)
google-cloud-firestore>=2.13.1
firebase-admin>=6.4.0
pyrebase4>=4.7.1

# AI Integration (Core only - Gemini for lightweight AI features)
google-generativeai>=0.3.2

# Security & Authentication
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Monitoring & Performance
psutil>=5.9.0
slowapi>=0.1.9
sentry-sdk>=1.40.0
pydantic-settings>=2.1.0

# Production Server
gunicorn>=21.2.0

# MCP Server (Demo Validation)
mcp>=1.0.0

# Lightweight Image Processing (QR Codes Only)
qrcode[pil]>=7.4.2
pillow>=10.0.0

# Document Generation (PDF Reports)
reportlab>=4.0.0

# ============================================================================
# EXCLUDED HEAVY DEPENDENCIES (moved to separate microservices)
# ============================================================================
#
# OCR Service (separate container):
# - opencv-python-headless>=4.8.0
# - pytesseract>=0.3.10
# - easyocr>=1.7.0
# - pyzbar>=0.1.9
#
# AI Team Service (separate container):
# - autogen-agentchat>=0.4.0
# - autogen-ext>=0.4.0
# - openai>=1.3.0
#
# Analytics Service (separate container if needed):
# - pandas>=2.0.0
# ============================================================================
